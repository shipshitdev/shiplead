# 2026-04-23

## Implementation Session

- Product direction locked: Shiplead is a self-hosted, single-owner, multi-project CRM + lead + SDR system
- Implemented a Bun/Turbo monorepo modeled after `../shipcode`
- Added `apps/api`, `apps/web`, `apps/desktop`
- Added shared packages for domain types, SQLite persistence, sequence engine, channel drafts, enrichment, and bounded agent helpers
- API seeds demo data automatically and exposes dashboard, search, CRUD, inbox, sequences, enrichment, and reply-triage routes
- Desktop app now acts as an operator cockpit for queue, search, inbox, activities, contacts, companies, and pipeline
- Web app now renders the same workspace snapshot against the live API
- Fixed desktop boot flow so Electron now auto-starts the local API instead of requiring a separate manual backend step
- Fixed broken desktop styling by switching the renderer shell to the same ShipCode theme/template setup and explicitly sourcing renderer TSX files for Tailwind v4 utility generation
- Reworked the desktop UI from loose marketing-style cards into a ShipCode-like dense app shell: hidden-inset titlebar, fixed sidebar, compact stats, table rows, smaller badges, and tighter controls
- Switched desktop/web UI imports to the published `@shipshitdev/ui` package, added Tailwind sources for its npm dist entry, and removed the local `@shiplead/ui` workspace package
- Replaced the desktop "New Task" sidebar page/action with a ShipCode-style modal backed by `POST /tasks`, including project/brand/priority/channel/due fields and `⌘↩` submit
- Replaced the desktop "Search" page with a ShipCode-style command palette backed by `/search`, with command groups for search results, actions, navigation, and projects; `⌘K` opens the palette and `⌘N` opens the task modal
- Reworked the desktop navigation hierarchy so only Tasks/Inbox/Activities are global; the sidebar project list stays flat, and the selected project view has ShipCode-style tabs for Pipeline, Contacts, and Companies in that order, with Pipeline as the default view
- Updated project tabs to `Overview`, `Pipeline`, `Contacts`, `Companies`; Pipeline is now a lead-status kanban board backed by real dashboard leads, Contacts use best-effort LinkedIn/X profile avatars, and Companies use favicon avatars
- Refined Pipeline tab layout: removed the project metric cards from tab content, made kanban columns full-height/equal-weight, and added a ShipCode-style right overlay panel for clicked lead cards
- Added real desktop create flows for Companies and Contacts; contacts can also seed a manual lead into the selected project pipeline when project/offer/company context exists
- Added persisted pipeline drag-and-drop with `PATCH /leads/:id/status`, optimistic desktop updates, stage-change activity logging, and API test coverage
- Verified with `bun run typecheck`, `bun run test`, `bun run --cwd apps/web build`, `bun run --cwd apps/desktop build:code`, `bun run --cwd apps/api build`, `node apps/api/dist/index.js`, and live `curl` to `/health` and `/dashboard`

## Follow-Up

- Add real channel/provider integrations
- Add import flows and richer editing
- Add auth/workspace settings before any hosted or shared-user mode

## Session Start

- User started a new main session with `start the session`
- Loaded SOUL/USER/MEMORY, today and yesterday memory, `.agents` quick-start/preferences/session logs, and inbox backlog
- Session documenter is active for today

## Homebrew Desktop Packaging

- Added ShipCut-style Electron packaging for ShipLead with `apps/desktop/electron-builder.yml`
- Desktop release builds now bundle the local API into `Contents/Resources/api/index.mjs`, so cask installs do not depend on a source checkout
- Added GitHub CI and publish workflows for web deploy, Linux/macOS installer builds, GitHub release artifact upload, and automatic `shipshitdev/homebrew-tap` cask SHA updates
- Added `shiplead` cask to the local `homebrew-tap` repo and updated install docs/site command to `brew tap shipshitdev/tap && brew install --cask shiplead`
- Verified format, lint, typecheck, tests, web build, desktop build, bundled API health/dashboard responses, Ruby cask syntax, and unsigned arm64 DMG generation

## CLI Packaging

- Confirmed `../shipcode` has a full CLI in `apps/cli`, published as `@shipshitdev/shipcode` with the `shipcode` bin
- Added ShipLead CLI package at `apps/cli`, published as `@shipshitdev/shiplead` with the `shiplead` bin
- CLI commands cover local API serving, status, search, task list/create/status, company list/create, contact list/create, and lead status updates
- Publish workflow now has optional `publish_cli` npm Trusted Publisher job
- Verified CLI build, direct compiled CLI smoke tests against temp SQLite data, CLI `serve` health check, and full workspace format/lint/typecheck/test/build paths

## Landing Page + npm Publish

- Updated the landing hero/install block to foreground the Homebrew desktop install and `npx @shipshitdev/shiplead` CLI path
- Pushed ShipLead source changes to `origin/master` in commits `caf70b8` and `a0b6623`
- Published `@shipshitdev/shiplead@0.0.1` to npm as a public package
- Verified the public package from a clean temp directory with `npm view @shipshitdev/shiplead` and `npx -y @shipshitdev/shiplead status --json`
